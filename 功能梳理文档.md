# 月舒（WomenMoon）功能梳理文档

> iOS 女性健康管理应用 | 平台：iOS 17.0+ | 语言：Swift 5.9 | UI：SwiftUI | 持久化：SwiftData | AI：DeepSeek API

---

## 一、项目总览

**月舒（WomenMoon）** 是一款面向女性用户的综合性健康管理 iOS 应用，结合现代医学、中医节气养生与 AI 智能分析，提供从经期追踪、营养管理、运动规划、情绪记录、病历管理到 AI 健康助手的全方位服务。

### 技术架构

| 层级 | 技术选型 |
|------|---------|
| UI | SwiftUI（iOS 17.0+） |
| 数据持久化 | SwiftData（本地） + CloudKit（云端） |
| 健康数据 | Apple HealthKit |
| AI 引擎 | DeepSeek API（OpenAI 兼容格式） |
| 密钥存储 | iOS Keychain |
| 项目生成 | XcodeGen（project.yml → .xcodeproj） |

---

## 二、功能模块详解

### 1. 首页（Home）

**文件：** [Views/Home/HomeView.swift](Views/Home/HomeView.swift)

首页是用户进入应用后的核心仪表盘，汇总展示所有关键信息：

- **用户欢迎区** — 显示用户姓名和当前日期（中文格式）
- **周期阶段卡片** — 显示当前所处周期阶段（经期/卵泡期/排卵期/黄体期），包含阶段图标、名称、当前天数（如"第 14 天"），以及阶段说明
- **今日推荐** — 根据当前周期阶段智能推荐今日运动方案（如"温和瑜伽 + 散步 30分钟"）和饮食建议（如"多补铁：红肉/菠菜/红枣"）
- **今日概览** — 三个核心指标卡片：
  - 步数（同步自 Apple Health）
  - 今日摄入热量（kcal，根据餐食记录计算）
  - 饮水杯数（根据餐食记录估算）
- **节气养生卡片** — 显示当前节气名称、时令食材推荐（横向滚动）、健康小贴士
- **快捷操作按钮** — 四个一键入口：记饮食、记心情、记体重、记补剂
- **Sheet 弹出表单** — 快速记录心情（选择心情 emoji + 情绪标签）、快速记录餐食（选择食物 + 分量）、快速记录体重（录入数值 + 日期）、快速记录补剂（名称 + 剂量 + 时间）

### 2. 经期周期追踪（Cycle）

**文件：** [Views/Cycle/CycleTrackerView.swift](Views/Cycle/CycleTrackerView.swift) | [Services/CycleCalculator.swift](Services/CycleCalculator.swift)

核心周期管理功能，包含以下子模块：

#### 2.1 周期阶段概览
- 四个周期阶段的可视化展示（经期 🩸 → 卵泡期 🌿 → 排卵期 ⭐ → 黄体期 🌙）
- 当前阶段高亮显示
- 显示周期长度、经期长度、预测下次经期开始日期

#### 2.2 经期日历
- 月历网格视图（周一起始），支持翻月
- 经期日以粉色实心标记，预测经期日以浅粉色标记，排卵期以橙色标记，今天以粉色圆环标记
- 图例说明（经期 / 预测经期 / 排卵期）

#### 2.3 身体信号提示
- 根据当前周期阶段展示四项身体指标：
  - 能量水平（⚡1-4 级）
  - 代谢率（正常 / 加速 / 最高）
  - 情绪敏感度（😢→🥰）
  - 运动耐受力（低→最佳表现）

#### 2.4 经期记录管理
- 手动记录：开始日期、结束日期、经量（1-5 级滑块）、症状标签（痛经/腹胀/疲劳/头痛等 11 种）、备注
- 从 Apple Health 自动同步经期数据（6 个月范围）
- 最近记录列表，显示日期、症状、经量

#### 2.5 周期计算引擎（CycleCalculator）
- **预测下次经期开始日** — 基于上次开始日 + 周期长度
- **预测排卵日** — 周期长度 - 黄体期长度
- **计算当前天在周期中的位置** — dayOfCycle
- **判断当前周期阶段** — 归一化天数 → 四个阶段
- **生成 3 个月预测列表** — 生成未来 3 个月的经期预测记录
- **受孕窗口期计算** — 排卵日前 5 天到后 1 天
- **各阶段日期范围** — 获取指定阶段在周期中的起止日期

### 3. 饮食管理（Nutrition）

**文件：** [Views/Nutrition/NutritionView.swift](Views/Nutrition/NutritionView.swift) | [Utils/NutritionCalculator.swift](Utils/NutritionCalculator.swift) | [Models/FoodItem.swift](Models/FoodItem.swift)

#### 3.1 热量与宏量营养素追踪
- **圆环进度图** — 今日摄入/目标热量的百分比进度环
- **宏量营养素条** — 蛋白质（绿色）、脂肪（橙色）、碳水（蓝色）的实际摄入 vs 目标对比进度条
- 自动计算目标：基于用户资料（体重、身高、年龄）通过 Mifflin-St Jeor 公式计算 BMR → TDEE → 按目标调整（减重 -500kcal / 减脂 -300kcal / 增肌 +300kcal / 保持不变）

#### 3.2 餐食记录
- 按餐食类型分类展示（早餐/午餐/晚餐/加餐）
- 每餐显示食物名称、克数、热量
- 通过底部工具栏 "+" 菜单操作：记录餐食 / 食材库

#### 3.3 食物数据库
- 内置食物库（[Resources/FoodDatabase.json](Resources/FoodDatabase.json)），包含 ~60+ 种常见食物
- 分类：主食、肉类、海鲜、蛋类、奶制品、蔬菜、水果、坚果、饮品、零食、补剂、调料
- 每 100g 营养数据：热量、蛋白质、脂肪、碳水、纤维素
- 支持自定义食物（isCustom 标记），通过 CloudKit 跨设备同步

#### 3.4 周期化饮食建议
- 根据当前周期阶段提供差异化饮食建议：
  - **经期**：补充铁质（红肉/菠菜/红枣），维生素 C 帮助铁吸收；避免生冷食物和冰饮
  - **卵泡期**：增加蛋白质 + 新鲜蔬果；减少精制糖和加工食品
  - **排卵期**：多摄入抗氧化食物（蓝莓/坚果/深色蔬菜）；控制盐分
  - **黄体期**：补充镁和 B6（坚果/香蕉/深绿蔬菜），适当增加碳水；控制甜食欲望

#### 3.5 营养计算器（NutritionCalculator）
- BMR 计算（Mifflin-St Jeor 公式，女性版）
- TDEE 计算（5 个活动水平 × BMR）
- 按目标计算每日热量目标
- 宏量营养素配比（减重 高蛋白、减脂 高蛋白低碳水、增肌 高碳水、保持 均衡）
- 周期调整营养（经期 +100kcal +铁/镁，黄体期 +150kcal +镁/B6）
- 食物热量计算（根据每 100g 营养数据和食用克数计算实际摄入）

### 4. 运动规划（Exercise）

**文件：** [Views/Exercise/ExerciseView.swift](Views/Exercise/ExerciseView.swift) | [Models/ExercisePlan.swift](Models/ExercisePlan.swift)

#### 4.1 周期运动指南
- 根据当前周期阶段展示运动耐受力等级和推荐运动
- 四个阶段的预设运动方案：

| 周期阶段 | 运动类型 | 强度 | 推荐项目 |
|---------|---------|------|---------|
| 经期 | 温和运动 | 低 | 温和瑜伽 20min · 拉伸 15min · 散步 30min |
| 卵泡期 | 力量 + HIIT | 高 | 力量训练 3×12 · HIIT 20min · 跑步 30min |
| 排卵期 | 高强度 | 最高 | 高强度力量 4×8 · HIIT 25min · 动感单车 40min |
| 黄体期 | 中等有氧 | 中 | 中等有氧 35min · 普拉提 30min · 游泳 30min |

#### 4.2 本周计划
- 显示本周运动计划列表，每项含日期（中文星期）、项目名称、时长、强度
- 支持运动打卡（点击完成按钮切换状态）
- 空状态提示"AI 生成计划"

#### 4.3 各阶段推荐展开
- 按四个周期阶段可展开/折叠查看各自的推荐运动
- 每项推荐显示：运动名称、时长、运动类型标签

#### 4.4 运动统计
- **连续运动天数** — 从 SwiftData 中计算连续完成天数
- **本周完成次数** — 本周已完成 / 本周总计
- **本月总计次数** — 本月已完成运动总数

#### 4.5 AI 计划生成器（PlanGeneratorView）
- 训练侧重选择：综合 / 减脂 / 增肌 / 塑形 / 柔韧 / 减压
- 提示："根据你的周期阶段和目标生成个性化运动方案"
- 调用 AI API（TODO 中）

#### 4.6 运动类型
13 种运动类型：瑜伽、普拉提、力量训练、HIIT、有氧、拉伸、散步、跑步、游泳、骑行、舞蹈、冥想、休息

### 5. AI 智能助手（AIAssistant）

**文件：** [Views/AIAssistant/AIAssistantView.swift](Views/AIAssistant/AIAssistantView.swift) | [Services/AIService.swift](Services/AIService.swift)

#### 5.1 对话助手"小月"
- 聊天界面（聊天气泡形式），用户头像 + AI 助手头像（月亮）
- 助手开场白介绍自身能力（解读身体信号、分析饮食营养、运动建议、情绪疏导）
- 自动关联用户上下文（周期阶段、年龄、运动目标、BMI）
- DeepSeek API 驱动，model: `deepseek-chat`
- 快捷问题推荐（横向滚动胶囊按钮）：
  - "我今天适合做什么运动？"
  - "经期腹痛怎么办？"
  - "推荐今天的食谱"
  - "为什么最近情绪波动大？"
  - "排卵期有什么注意事项？"
  - "帮我分析一下我的饮食"

#### 5.2 AI 服务能力（AIService）
核心 AI 服务提供以下 5 个专业分析接口：

| 方法 | 功能 | 输入 | 输出 |
|------|------|------|------|
| `analyzeNutrition()` | 营养分析 | 食物名称 + 分量 | 热量/蛋白质/脂肪/碳水/纤维素/维生素列表/备注 |
| `interpretBodySignal()` | 身体信号解读 | 信号描述 + 周期阶段 + 近期症状 | 科学解读 + 健康建议（不提供医学诊断） |
| `analyzeEmotion()` | 情绪分析 | 心情 + 情绪标签 + 周期阶段 + 备注 | 总结/可能原因/建议/是否与周期相关 |
| `chat()` | AI 助手对话 | 用户消息 + 上下文 | 个性化建议回复 |
| `analyzeMedicalRecord()` | 医疗记录分析 | 病例标题 + 诊断 | 通俗语言总结分析 |
| `getDailyDietAdvice()` | 每日饮食建议 | 周期阶段 + 目标 | 今日饮食建议 |

- API 端点：`https://api.deepseek.com/v1/chat/completions`
- API Key：优先读取环境变量 `DEEPSEEK_API_KEY`，其次读取 Keychain
- 自动从 Markdown 代码块中提取 JSON 返回结果

### 6. 情绪管理（Emotion）

**文件：** [Views/Emotion/EmotionTrackerView.swift](Views/Emotion/EmotionTrackerView.swift)

#### 6.1 心情打卡
- 5 级心情评分（😫 很差 / 😔 不太好 / 😐 一般 / 😊 不错 / 🥰 很好）
- 12 种情绪标签可选（开心、焦虑、易怒、悲伤、疲惫、精力充沛、平静、压力大、胀气、腹痛、头痛、失眠）
- 关联当前周期阶段和周期天数
- 支持记录触发因素和备注

#### 6.2 7 天情绪趋势
- 柱状图展示近 7 天情绪变化
- 每天以 emoji + 情绪值高度柱 + 中文星期标签呈现
- 颜色编码：绿色（很好）→ 红色（很差）

#### 6.3 周期-情绪关联
- 根据当前阶段科普情绪特点：
  - 经期：激素低谷，可能疲劳低落
  - 卵泡期：精力充沛，心情好
  - 排卵期：自信提升，魅力四射
  - 黄体期：可能情绪波动，注意自我关怀

#### 6.4 AI 情绪分析
- 基于近期情绪记录调用 AI 分析情绪模式
- 提供科学的调节建议

#### 6.5 正念 & 冥想（MindfulnessView）
- **呼吸练习** — 4-7-8 呼吸法，带动画的呼吸圆环，可计数轮数
- **身体扫描冥想** — 5 分钟引导练习
- **感恩日记** — 记录三件感恩的事 + 保存

### 7. 健康数据管理（HealthMetrics）

**文件：** [Views/Health/HealthMetricsView.swift](Views/Health/HealthMetricsView.swift) | [Services/HealthKitService.swift](Services/HealthKitService.swift) | [Models/HealthMetric.swift](Models/HealthMetric.swift)

#### 7.1 多指标追踪
支持 7 种身体指标追踪：
- 体重（kg）· 体脂率（%）· 腰围（cm）· 臀围（cm）· 大腿围（cm）· 手臂围（cm）· 胸围（cm）

#### 7.2 趋势图表
- 选中指标后展示趋势柱状图，支持 7 天 / 30 天 / 90 天时间范围切换
- 统计卡片：最低值、最高值、平均值
- 最近记录列表，标注数据来源（手动录入 / Apple Health）

#### 7.3 Apple HealthKit 集成（HealthKitService）
- **读取数据类型**：步数、心率、睡眠分析、经期记录、体重、体脂率、身高
- **写入数据类型**：经期记录、体重、体脂率
- **数据同步策略**：
  - 经期：同步 6 个月历史数据 → SwiftData（去重：同日期 + 非预测记录）
  - 体重：同步 30 天历史数据 → SwiftData（去重：同日期 + HealthKit 来源）
  - 步数/心率/睡眠：仅显示，不存入本地数据库
- 使用 `withCheckedThrowingContinuation` 将 HealthKit 回调桥接为 async/await

### 8. 病历管理（MedicalRecords）

**文件：** [Views/MedicalRecords/MedicalRecordsView.swift](Views/MedicalRecords/MedicalRecordsView.swift) | [Models/MedicalRecord.swift](Models/MedicalRecord.swift)

#### 8.1 病历记录
- 记录内容：标题、日期、类别（妇科/皮肤科/内分泌科等 9 类）、医院、医生、诊断、处方
- 支持添加多个自定义标签
- 支持图片附件（拍照/相册导入）
- 收藏标记

#### 8.2 快速操作
- 拍照导入（调用相机）
- 手动录入
- AI 分析（调用 DeepSeek 分析病例，用通俗语言总结关键信息）

#### 8.3 分类筛选
- 水平滚动的分类胶囊按钮（全部 / 妇科 / 皮肤科 / 内分泌科 / 消化科 / 心内科 / 骨科 / 神经科 / 全科 / 其他）
- 支持按标题搜索

#### 8.4 病历卡片展示
- 显示类别标签、日期、标题、诊断、AI 分析摘要（最多 2 行）、标签

### 9. 节气养生（SeasonalWellness）

**文件：** [Views/SeasonalWellness/SeasonalWellnessView.swift](Views/SeasonalWellness/SeasonalWellnessView.swift) | [Services/SeasonalWellnessService.swift](Services/SeasonalWellnessService.swift) | [Utils/SeasonalTerms.swift](Utils/SeasonalTerms.swift)

#### 9.1 24 节气数据引擎
- 内置 2026-2027 年 24 节气日期数据库（每年 1-2 天浮动）
- 每个节气包含详细养生数据：
  - 节气描述
  - 饮食建议（3-5 条）
  - 起居建议（2-4 条）
  - 运动导引建议
  - 时令食材推荐
  - 健康小贴士

#### 9.2 当前节气卡片
- 大标题显示节气名称
- 节气描述文字
- 季节标签：春季/夏季/秋季/冬季 + 五行属性（木/火/金/水）+ 养护脏器（肝/心/肺/肾）

#### 9.3 养生建议
- **饮食调养** — 节气饮食建议 + 时令食材 emoji 展示（如 🍖羊肉、🫐红枣、🍐梨等 30+ 种食材映射）
- **起居调养** — 睡眠、保暖、作息建议
- **运动导引** — 节气运动建议
- **经络穴位** — 按季节推荐重点经络和穴位（如春季：太冲·行间；夏季：神门·少府；秋季：太渊·尺泽；冬季：太溪·涌泉）+ 按摩 tips

#### 9.4 24 节气一览
- 4 列网格展示全年 24 节气
- 当前节气高亮（绿色背景）

#### 9.5 四季养生
- 获取当前季节（春/夏/秋/冬）
- 季节关联五行和脏器：
  - 春属木 → 养肝、胆
  - 夏属火 → 养心、小肠
  - 秋属金 → 养肺、大肠
  - 冬属水 → 养肾、膀胱

### 10. 补剂管理（Supplement）

**文件：** [Models/SupplementRecord.swift](Models/SupplementRecord.swift) | 快捷操作在 [HomeView.swift](Views/Home/HomeView.swift)

- 记录补剂名称、剂量（如 "500mg"）、单位（片/粒/毫升）、数量
- 按服用时间分类（早晨/中午/晚上/睡前）
- 关联周期阶段（如经期补铁、黄体期补镁）
- 服用状态追踪（已服用/未服用）
- 通过 CloudKit 同步补剂记录

### 11. 设置（Settings）

**文件：** [Views/Settings/SettingsView.swift](Views/Settings/SettingsView.swift)

#### 11.1 个人资料
- 编辑姓名、出生日期、身高（滑块 130-200cm）
- 年龄自动计算

#### 11.2 周期设置
- 周期长度（默认 28 天）、经期长度（默认 5 天）
- 目标设置（减重/减脂/增肌/保持，多选）
- 运动水平设置（久坐/轻度/中度/高度/极高，5 级，带描述）

#### 11.3 Apple Health 连接
- 已连接状态显示

#### 11.4 AI 设置
- 当前模型显示（DeepSeek Chat）
- API Key 配置（Keychain 安全存储 + DeepSeek 平台链接）

#### 11.5 数据管理
- 导出数据
- 清除所有数据（含确认弹窗，删除 SwiftData 中 9 个模型的所有记录）

#### 11.6 通知提醒
- 经期预测提醒、补剂提醒、运动提醒、饮食记录提醒、心情打卡提醒
- 提醒时间设置（早晨 8:00 / 晚间 21:00）

#### 11.7 开发者工具
- 重置引导流程（重新进入 Onboarding）

### 12. 引导流程（Onboarding）

**文件：** [Views/Onboarding/OnboardingView.swift](Views/Onboarding/OnboardingView.swift)

5 步骤引导流程（页面滑动 + 索引指示器）：

1. **欢迎页** — 品牌展示（月亮图标 + "月舒 Women-Moon"），四个核心功能卖点：全周期健康管理、节气养生智慧、情绪陪伴与引导、AI 健康助手
2. **基本信息** — 姓名、出生日期、身高（滑块）
3. **目标设置** — 目标选择（减重⚖️ / 减脂🔥 / 增肌💪 / 保持✨），带勾选卡片
4. **Apple Health 授权** — 介绍数据隐私（本地存储、可管理权限、不上传云端），一键授权并继续
5. **周期设置** — 规律性选择（规律 / 不规律），规律时设置周期长度（21-35 天滑块）和经期长度（2-10 天滑块）；设置末次经期开始日；一键从 Apple Health 导入历史经期数据；完成后创建 UserProfile 并生成 3 个月预测

---

## 三、数据模型总览

| 模型 | 表名 | 核心字段 | 说明 |
|------|------|---------|------|
| **UserProfile** | 用户档案 | name, birthDate, cycleLength(28), periodLength(5), lutealLength(14), goals, height, activityLevel, lastCycleStartDate | 单用户档案，含 age/bmi/currentCyclePhase 计算属性 |
| **CycleRecord** | 经期记录 | startDate, endDate, phase, symptoms(11种), flowIntensity(1-5), isPredicted | 区分真实记录和预测记录 |
| **HealthMetric** | 健康指标 | date, type(7种), value, unit, source(手动/HealthKit) | 点状测量数据 |
| **FoodItem** | 食物库 | name, category(12类), caloriesPer100g, protein, fat, carbs, fiber, vitamins, isCustom | 内置 + 自定义食物 |
| **MealRecord** | 餐食记录 | date, mealType(4类), foods([FoodEntry]), totalCalories/macros | 每餐含多个 FoodEntry |
| **EmotionRecord** | 情绪记录 | date, mood(1-5), emotions(12种), trigger, cyclePhase, dayOfCycle | 关联周期阶段 |
| **ExercisePlan** | 运动计划 | date, cyclePhase, exercises([Exercise]), intensity(3级), durationMinutes, isCompleted | 含 static recommended(for:) |
| **MedicalRecord** | 病历记录 | date, title, category(9类), hospital, doctor, diagnosis, prescription, images, aiAnalysis, tags | 支持 AI 分析 |
| **SupplementRecord** | 补剂记录 | date, supplementName, dosage, unit, quantity, timeOfDay(4时段), isTaken, cyclePhase | 服用状态追踪 |

---

## 四、枚举类型一览（Utils/Constants.swift）

| 枚举 | 值 |
|------|---|
| **CyclePhase** | 经期(🔥) → 卵泡期(🌿) → 排卵期(⭐) → 黄体期(🌙) |
| **Goal** | 减重 / 减脂 / 增肌 / 保持 |
| **ActivityLevel** | 久坐不动 / 轻度活动 / 中度活动 / 高度活跃 / 极高活跃 |
| **MetricType** | 体重 / 体脂率 / 腰围 / 臀围 / 大腿围 / 手臂围 / 胸围 |
| **DataSource** | 手动录入 / Apple Health |
| **MealType** | 早餐 / 午餐 / 晚餐 / 加餐 |
| **Mood** | 😫很差(1) → 😔不太好(2) → 😐一般(3) → 😊不错(4) → 🥰很好(5) |
| **EmotionTag** | 开心/焦虑/易怒/悲伤/疲惫/精力充沛/平静/压力大/胀气/腹痛/头痛/失眠 |
| **Symptom** | 痛经/腹胀/疲劳/头痛/长痘/乳房胀痛/腰酸/恶心/腹泻/失眠/食欲增加 |
| **Intensity** | 低强度 / 中等强度 / 高强度 |
| **TimeOfDay** | 早晨 / 中午 / 晚上 / 睡前 |
| **FoodCategory** | 主食/肉类/海鲜/蛋类/奶制品/蔬菜/水果/坚果/饮品/零食/补剂/调料 |
| **MedicalCategory** | 妇科/皮肤科/内分泌科/消化科/心内科/骨科/神经科/全科/其他 |
| **ExerciseType** | 瑜伽/普拉提/力量/HIIT/有氧/拉伸/散步/跑步/游泳/骑行/舞蹈/冥想/休息 |
| **Season** | 春(木,肝/胆) / 夏(火,心/小肠) / 秋(金,肺/大肠) / 冬(水,肾/膀胱) |

---

## 五、配色系统

| 色彩 | Hex | 用途 |
|------|-----|------|
| 主色-玫瑰粉 | `#E91E63` | 品牌主色、Tab tint、按钮、选中态 |
| 辅助色-紫色 | `#9C27B0` | AI 相关功能、情绪模块 |
| 经期红 | `#E74C3C` | 经期阶段标识 |
| 卵泡绿 | `#2ECC71` | 卵泡期阶段标识 |
| 排卵橙 | `#F39C12` | 排卵期阶段标识 |
| 黄体紫 | `#9B59B6` | 黄体期阶段标识 |
| 饮食橙 | `#FF5722` | 热量相关展示 |
| 运动绿 | `#4CAF50` | 运动相关展示 |
| 数据蓝 | `#2196F3` | 健康指标展示 |

---

## 六、外部服务集成

| 服务 | 用途 | 配置 |
|------|------|------|
| **DeepSeek API** | AI 分析引擎 | `https://api.deepseek.com/v1`，model: `deepseek-chat`，API Key 通过环境变量或 Keychain |
| **Apple HealthKit** | 健康数据读写 | 读：步数/心率/睡眠/经期/体重/体脂/身高；写：经期/体重/体脂 |
| **CloudKit** | 云端同步 | Container: `iCloud.com.womenmoon.app`，同步：自定义食物 + 补剂记录 |
| **Keychain** | 密钥安全存储 | DeepSeek API Key 存储/读取/删除 |

---

## 七、项目文件结构

```
women-moon/
├── WomenMoonApp.swift          # @main 入口，SwiftData Schema 注册
├── ContentView.swift           # 根视图（Onboarding / MainTabView 切换）+ Color hex 扩展
├── AppState.swift              # 全局状态（ObservableObject）
├── project.yml                 # XcodeGen 项目配置
├── CLAUDE.md                   # 开发者文档
├── Models/                     # SwiftData @Model 数据模型
│   ├── UserProfile.swift
│   ├── CycleRecord.swift
│   ├── EmotionRecord.swift
│   ├── ExercisePlan.swift      # + Exercise 结构体 + ExerciseType 枚举
│   ├── FoodItem.swift          # + FoodEntry / VitaminContent 结构体
│   ├── HealthMetric.swift      # + MetricType 扩展（单位/图标）
│   ├── MedicalRecord.swift
│   └── SupplementRecord.swift
├── Services/                   # 业务服务层（全部 async/await）
│   ├── AIService.swift         # DeepSeek API 客户端 + 分析数据模型
│   ├── HealthKitService.swift  # Apple Health 读写
│   ├── CycleCalculator.swift   # 周期计算引擎
│   ├── CloudKitService.swift   # iCloud 同步
│   └── SeasonalWellnessService.swift  # 节气查询 + 养生建议
├── Utils/                      # 工具类 & 常量
│   ├── Constants.swift         # 所有枚举 + 常量（API URL、默认值等）
│   ├── NutritionCalculator.swift  # BMR/TDEE/宏量营养素/周期调整
│   ├── SeasonalTerms.swift     # 24 节气数据库（2026-2027）
│   ├── DateExtensions.swift    # 日期扩展（格式化/周期计算/节气查询）
│   ├── StringExtensions.swift  # 字符串扩展（截断/空白判断）
│   └── ColorExtensions.swift   # 品牌色 + 阶段色
├── Views/                      # 视图层
│   ├── Home/HomeView.swift     # 首页仪表盘 + 快捷操作 Sheet
│   ├── Onboarding/OnboardingView.swift  # 5 步引导流程
│   ├── Cycle/CycleTrackerView.swift     # 周期追踪
│   ├── Nutrition/NutritionView.swift    # 饮食管理
│   ├── Exercise/ExerciseView.swift      # 运动规划
│   ├── AIAssistant/AIAssistantView.swift  # AI 对话助手
│   ├── Emotion/EmotionTrackerView.swift   # 情绪管理 + 正念
│   ├── Health/HealthMetricsView.swift     # 健康数据
│   ├── MedicalRecords/MedicalRecordsView.swift  # 病历管理
│   ├── SeasonalWellness/SeasonalWellnessView.swift  # 节气养生
│   └── Settings/SettingsView.swift       # 设置 + KeychainHelper
├── Resources/
│   └── FoodDatabase.json       # 内置食物营养数据（~60+ 种）
└── ViewModels/                 # 空目录（预留）
```

---

## 八、数据流架构

```
┌─────────────────────────────────────────────────────┐
│                    用户界面 (SwiftUI)                  │
│   Home · Cycle · Nutrition · Exercise · AI · ...     │
│         @EnvironmentObject(AppState)                 │
└──────────┬────────────────────────────┬──────────────┘
           │ @Query                    │ async/await
           ▼                           ▼
┌──────────────────┐    ┌─────────────────────────────┐
│   SwiftData       │    │        Services              │
│  (本地持久化)      │◄───│  AIService → DeepSeek API    │
│  9 个 Model       │    │  HealthKitService → Apple    │
│  去重 + 同步      │    │  CloudKitService → iCloud    │
└──────────────────┘    │  CycleCalculator (纯计算)     │
                        │  SeasonalWellnessService      │
                        └─────────────────────────────┘
```

- 首页启动时：从 HealthKit 同步步数/体重/经期（增量去重写入 SwiftData）
- 各页面通过 `@Query` 读取 SwiftData 数据，通过 `@Environment(\.modelContext)` 写入
- AI 功能通过 `AIService.shared` 调用 DeepSeek API，解析 JSON 结果
- CloudKit 同步仅针对自定义食物和补剂记录两类非敏感数据
- 用户偏好（如是否完成引导）通过 `UserDefaults` 持久化
- API Key 通过 Keychain 安全存储
```

